    import XCTest
    @testable import UnwrapOrThrowOperator

    final class UnwrapOrThrowOperatorTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(UnwrapOrThrowOperator().text, "Hello, World!")
        }
    }
